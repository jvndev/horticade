main() {
	String pretifyAuthErrorCode(String message) {
		return message
			.split('-')
			.map((e) => e.replaceRange(0, 1, e[0].toUpperCase()))
			.fold('', (str, e) => '$str $e');
	}

	print(pretifyAuthErrorCode('test1-test2'));
}
